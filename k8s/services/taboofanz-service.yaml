apiVersion: v1
kind: Service
metadata:
  name: taboofanz-service
  namespace: fanz-ecosystem
  labels:
    app: taboofanz
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: taboofanz
