apiVersion: v1
kind: Service
metadata:
  name: transfanz-service
  namespace: fanz-ecosystem
  labels:
    app: transfanz
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: transfanz
