apiVersion: apps/v1
kind: Deployment
metadata:
  name: transfanz
  namespace: fanz-ecosystem
  labels:
    app: transfanz
    platform: fanz
    tier: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: transfanz
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: transfanz
        platform: fanz
        tier: frontend
    spec:
      containers:
      - name: transfanz
        image: registry.digitalocean.com/fanz/transfanz:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        env:
        - name: PLATFORM_NAME
          value: "TransFanz"
        - name: PLATFORM_PORT
          value: "5004"
        envFrom:
        - configMapRef:
            name: fanz-config
        - secretRef:
            name: fanz-secrets
        resources:
          requests:
            cpu: 200m
            memory: 384Mi
          limits:
            cpu: 500m
            memory: 1Gi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5
        volumeMounts:
        - name: uploads
          mountPath: /app/uploads
      volumes:
      - name: uploads
        persistentVolumeClaim:
          claimName: fanz-uploads-pvc
      imagePullSecrets:
      - name: digitalocean-registry
