apiVersion: apps/v1
kind: Deployment
metadata:
  name: boyfanz
  namespace: fanz-ecosystem
  labels:
    app: boyfanz
    platform: fanz
    tier: frontend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: boyfanz
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: boyfanz
        platform: fanz
        tier: frontend
    spec:
      containers:
      - name: boyfanz
        image: registry.digitalocean.com/fanz/boyfanz:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        - containerPort: 3001
          name: websocket
        env:
        - name: PLATFORM_NAME
          value: "BoyFanz"
        - name: PLATFORM_PORT
          value: "5001"
        envFrom:
        - configMapRef:
            name: fanz-config
        - secretRef:
            name: fanz-secrets
        resources:
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 2Gi
        livenessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: uploads
          mountPath: /app/uploads
      volumes:
      - name: uploads
        persistentVolumeClaim:
          claimName: fanz-uploads-pvc
      imagePullSecrets:
      - name: digitalocean-registry
